package {{.Package}}_repository

import (
	"context"

	"{{.ProtoPkg}}"
)

// {{.Service.GoName}}Repository defines the data access interface
type {{.Service.GoName}}Repository interface {
	{{range .Service.Methods}}{{.GoName}}(context.Context, *{{.Input.GoIdent.GoName}}) (*{{.Output.GoIdent.GoName}}, error)
	{{end}}
}

// {{initialLower .Service.GoName}}Repository implements the repository interface
type {{initialLower .Service.GoName}}Repository struct {
	// Add data source dependencies (e.g., *gorm.DB, *mongo.Client)
}

// New{{.Service.GoName}}Repository creates a new repository instance
func New{{.Service.GoName}}Repository(/* dependencies */) {{.Service.GoName}}Repository {
	return &{{initialLower .Service.GoName}}Repository{
		// Initialize dependencies
	}
}

{{range .Service.Methods}}
// {{.GoName}} handles data access for {{.GoName}}
func (r *{{initialLower $.Service.GoName}}Repository) {{.GoName}}(ctx context.Context, req *{{.Input.GoIdent.GoName}}) (*{{.Output.GoIdent.GoName}}, error) {
	// Implement data access logic (DB operations, cache, etc.)
	return &{{.Output.GoIdent.GoName}}{}, nil
}
{{end}}